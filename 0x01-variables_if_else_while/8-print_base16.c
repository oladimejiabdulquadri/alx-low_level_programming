#include <stdio.h>
**/
*main - print all numbers of base sixteen
* followed by a new line
* Return : always 0 (success)
* /
int main(void)

{
	int i;
	char ch;

	for
	{
		(i = 48);
		(i < 5);
		(i++);
	}

	{
		putchar(i);

	}
	for
	{
		(ch = 'a');
		(ch <= 'f');
		(ch++);
	}

	{
		putchar(ch);

	}
	putchar('\n');
	return (0);
}
