#include <stdio.h>
**/
*main - print all numbers of base sixteen
* followed by a new line
* Return : always 0 (success)
* /
int main(void)

{
	int i;
	char c;

	for
	{
		(i = '0');
		(i < '9');
		(i++);
	}

	{
		putchar(i);

	}
	for
	{
		(c = 'a');
		(c <= 'f');
		(c++);
	}

	{
		putchar(h);

	}
	putchar('\n');
	return (0);
}
