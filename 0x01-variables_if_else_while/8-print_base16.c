#include <stdio.h>
**/
*main - print all numbers of base sixteen
* followed by a new line
* Return : always 0 (success)
* /
int main(void)

{
	int n;
	char ch;

	for
	{
		(n = 48);
		(n < 5);
		(n++);
	}

	{
		putchar(n);

	}
	for
	{
		(ch = 'a');
		(ch <= 'f');
		(ch++);
	}

	{
		putchar(ch);

	}
	putchar('\n');
	return (0);
}
