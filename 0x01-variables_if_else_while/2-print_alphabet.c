#include <stdio.h>
/**
  * main - print
  * followed by coding
  * Return : always zero (success)
  */
int main(void)

{

	char ch;

	for (ch = 'a'; ch <= 'z'; ch++
			)

	{
		putchar(ch);

	}

	putchar('\n');

	return (0);
}
