#include "main.h"
/**
 *_isupper - find upper case letters
 *@c: the char in question
 * Return: return 1, otherwise 0
 */
int _isupper(int c)
{
	if
		(c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
