#include "main.h"
/**
  *_isalpha - check for char alphabet
  *Return: 1 if lower or upper, 0 otherwise
  *@c: the char to be checked
  */
int _isalpha(int c)
{
	if
		((c >= 'a' && c <= 'z') || (c >= 'a' && c <= 'z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
