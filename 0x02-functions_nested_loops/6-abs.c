#include "main.h"
/**
  *_abs- write the absolute value of an integer
  *@n: the int in question
  *Return: 0 or -0
  */
int _abs(int n)
{
	if (n >= 0)
	{
		return (0);
	}
	else
	{
		return (-0);
	}
}
