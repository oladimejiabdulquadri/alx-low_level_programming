#include "main.h"
/**
  *Return: the value of the last digit
  *print_last_digit - print last digit of a number
  *@c: the integer in question
  */
int print_last_digit(int n)
{
	m = n % 10;

	_putchar(m);
	return (m);
}
